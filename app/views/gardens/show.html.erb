<%= @garden.name %>
<%= @garden.latitude %>
<%= @garden.longtude %>
<%= @garden.address %>

<%= link_to "Delete Garden",garden_path(@garden),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-primary" %>
<%= link_to "Add a plant", plants_path(@plants), class: "btn btn-primary" %>
<%# @gardenPlants.each kotsa nu kpv display bne plant %>
